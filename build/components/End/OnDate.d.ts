export default EndOnDate;
declare function EndOnDate({ id, onDate: { date, options, }, handleChange, translations, }: {
    id: any;
    onDate: {
        date: any;
        options: any;
    };
    handleChange: any;
    translations: any;
}): any;
declare namespace EndOnDate {
    namespace propTypes {
        const id: any;
        const onDate: any;
        const handleChange: any;
        const translations: any;
    }
}
