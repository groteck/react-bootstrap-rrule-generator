export default Start;
declare function Start({ id, start: { onDate, }, handleChange, translations, }: {
    id: any;
    start: {
        onDate: any;
    };
    handleChange: any;
    translations: any;
}): any;
declare namespace Start {
    namespace propTypes {
        const id: any;
        const start: any;
        const handleChange: any;
        const translations: any;
    }
}
