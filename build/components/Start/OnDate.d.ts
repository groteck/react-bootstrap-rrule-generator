export default StartOnDate;
declare function StartOnDate({ id, onDate: { date, options, }, handleChange, translations, }: {
    id: any;
    onDate: {
        date: any;
        options: any;
    };
    handleChange: any;
    translations: any;
}): any;
declare namespace StartOnDate {
    namespace propTypes {
        const id: any;
        const onDate: any;
        const handleChange: any;
        const translations: any;
    }
}
