export default RepeatDaily;
declare function RepeatDaily({ id, daily: { interval, }, handleChange, translations, }: {
    id: any;
    daily: {
        interval: any;
    };
    handleChange: any;
    translations: any;
}): any;
declare namespace RepeatDaily {
    namespace propTypes {
        const id: any;
        const daily: any;
        const handleChange: any;
        const translations: any;
    }
}
