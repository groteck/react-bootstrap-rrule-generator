export default RepeatYearlyOn;
declare function RepeatYearlyOn({ id, mode, on, hasMoreModes, handleChange, translations, }: {
    id: any;
    mode: any;
    on: any;
    hasMoreModes: any;
    handleChange: any;
    translations: any;
}): any;
declare namespace RepeatYearlyOn {
    namespace propTypes {
        const id: any;
        const mode: any;
        const on: any;
        const hasMoreModes: any;
        const handleChange: any;
        const translations: any;
    }
}
